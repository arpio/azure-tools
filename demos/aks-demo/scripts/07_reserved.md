# 07 — Reserved

This number is reserved to maintain consistency with a potential future
`deploy-castai/` variant of this repo (equivalent to `terraform/deploy-castai/`
in the EKS demo), where `07` would be used for CAST AI integration.

Keeping this placeholder ensures that if additional components are added to both
deployment configurations, the numbering stays aligned for easier side-by-side
comparison.
